# alias

Example: `alias: { '@lib': 'src/lib' }` — route imports from `@lib/now`.

- **Run:** `pnpm i && pnpm run build && pnpm test`
- **Dev:** `pnpm run dev` then GET `http://localhost:5173/api/health`
