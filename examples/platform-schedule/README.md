# Platform — `vitek schedule`

Defines `vitek.schedule.mjs` with `defineSchedule` from `vitek-plugin/scheduler` and runs tasks via `vitek schedule run`.

See [Events and Scheduler](../../docs/guide/events-scheduler.md).
