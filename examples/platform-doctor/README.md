# Platform — `vitek doctor`

Minimal app with `vitek.platform.json` enabling `features.doctor` and a sample `docs/overview.md` so the doctor CLI has project context inside this folder.

Run from this directory:

```bash
pnpm run doctor
```

See [Doctor Score CLI](../../docs/guide/doctor.md).
