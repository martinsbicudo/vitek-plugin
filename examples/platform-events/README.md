# Platform — typed event bus

Demonstrates `createEventBus` from `vitek-plugin/events` with in-process handlers and HTTP routes that emit and read audit entries.

See [Events and Scheduler](../../docs/guide/events-scheduler.md).
